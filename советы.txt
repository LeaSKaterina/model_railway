подсказки,замечания,советы которые можно послать нахуй C:

2)
можно сократить много чего следующим методом:
в начале общего load() сделать следующую штуку
	ifstream F;
    F.open(path);
    if (!F.is_open()) {
        cout<<"Error"<<endl;
        return;
    }

потом если нужно считать какой-то инт из файла
сделать функцию которая возвращает следующий по ходу инт примерно таким образом
int get_int_from_file(ifstream &F){
	//реализация считывания,если тут число или строка выбросить ошибку
	return //искомый инт;
} 

таким образом следующую штуку можно переписать вот так

int bufferNumberOfGoods;
F>>bufferNumberOfGoods;

int bufferNumverOfGoods = get_int_from_file(F);

так же чисто в теории и на свой страх и риск можно юзать следующую конструкцию
for(int i = 0 ; i < get_int_from_file(F) ; i++) .... - нельзя,сейчас затестил,но можно следующую
for(int i = 0; kolvo = get_int_from_file(F) ; i<kolvo ; i++) .... 

так же не нужно постоянно проверять,открыт ли файл и кидать ошибки,при надобности можно сдать метод считывающий строку или слово (так же нужна ошибка на конец файла при попытке считать из файла то,чего там нет)

3)Используя метод 2) следующий код модно упросить следующим методом

                int bufferNumberOfGoods;
                F>>bufferNumberOfGoods;
                Map::listOfStations.push_back(new FreightStation(bufferName, bufferNumberOfGoods));
                break;

				Map::listOfStations.push_back(new FreightStation(get_int_from_file(F),get_int_from_file(F)));
                break;
Но стоит опасаться следующей ловушки(только сейчас разобрался как эта ебанина работает,в своей лабе я это не использовал,не понял в чём ловушка)
Функции в этой функции вызываются справа налево,то есть
если в файле у нас идут числа 10 15 ,то в new FreightStation пойдут значения так: new FreightStation(15,10) !!! це важно и нужно учитывать


8)чуть скажу про исключения
по его словам на каждый тип ошибки должен быть свой класс,я же забил на это хуй
чё я,ёбнутый,на каждую ошибку писать по классу в такой лабе,поэтому написал один клаас и сдал